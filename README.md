# SETUP INSTRUCTIONS

## Requirements

   1. Git
   2. SQL Server Express
   3. Dotnet SDK Version X.X (TODO: Fill this out)

## Steps

   1. Run `git clone --recurse-submodules https://github.com/ITD333GroupB/Main.git`
   2. Navigate to the 'Main' directory
   3. Execute `run.bat`